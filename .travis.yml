language: ruby

cache: bundler

rvm:
  - truffleruby-head

before_install:
  - gem update --system

matrix:
  include:
    - rvm: 2.3.6
      script:
        - bundle exec danger
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-9.1.16.0
    - rvm: jruby-head
